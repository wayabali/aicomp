
import React from 'react';
import '../styles/nextButton.css'

const PreceButton = ({ onClick }) => {
  return <button className='PreceButton' onClick={onClick}>Précedent</button>;
};

export default PreceButton;
